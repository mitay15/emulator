[metadata]
name = aaps_emulator
version = 0.0.1
description = AAPS emulator tools

[options]
packages = find:
install_requires =
    pandas
    plotly
	numpy

[options.entry_points]
console_scripts =
    aaps-emulator = aaps_emulator.run:main
