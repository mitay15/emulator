[pytest]
testpaths = tests

markers =
    unit: fast unit tests
    integration: integration tests against AAPS blocks
    visual: tests that generate plots/images
    report: tests that generate HTML reports
